# Audio Trimmer Example

This example demonstrates the usage of the `audio_trimmer` package.

## Getting Started

To run this example:

```bash
flutter pub get
flutter run
```

## Features Demonstrated

- Basic audio trimmer overlay with default styling
- Customized trimmer with custom colors and dimensions
- Interactive play/pause with animated progress
- Multiple configurations side by side
